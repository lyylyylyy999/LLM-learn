# AI 应用工程学习路线

## 目标与执行原则

时间范围：2026 年 8 月底至 2027 年春节前后。

目标不是覆盖所有知识点，而是形成可展示、可测试、可解释的 AI 应用工程能力，并为 2027 年上半年的实习准备建立项目证据。路线按阶段管理，不按天排死；相邻阶段可以重叠，每周复盘可以调整节奏和补强项。路线不采用“先学完 Python 和后端，再接触模型”的瀑布式顺序；完成最少必要的 Python 基础后，先通过小型 LLM 任务建立真实模型交互经验，再让后端、数据库和生产工程逐步承载这条功能主线。

贯穿所有阶段的工程要求：Git 小步提交、类型标注、错误处理、日志、pytest、可复现配置、简短设计说明，以及对正确性、延迟、成本的基本度量。生产工程能力随功能逐步加入，不在前期一次性搭建复杂基础设施。

## 阶段 1 — Python 工程基础

**建议时间：** 2026 年 9 月至 10 月上旬

### 阶段目标

- 恢复并夯实能直接服务 AI/后端项目的 Python 工程能力。
- 建立“先写清行为，再用测试验证”的习惯。
- 能阅读、调试和修改中小型 Python 模块。

### 核心技能

- 类型标注、数据类、异常设计、迭代器/生成器、装饰器、上下文管理器。
- 文件与 JSON、模块与包、虚拟环境、配置和日志。
- pytest、夹具、参数化、模拟对象的适用边界、调试。
- async/await、并发与并行的基本区别。

### 阶段产出

- 3–5 个 L1/L2 小型工程任务，而非孤立语法题。
- 一个带清晰模块边界、类型标注、日志和测试的小型数据处理/客户端项目。
- 每个有代码量的任务都有代码审查，并按难度完成简版或完整版复盘。

### 退出标准

- 能独立将模糊需求拆成函数、数据模型、异常和测试用例。
- 能解释同步/异步选择，定位常见异常堆栈，并为正常、失败和边界路径编写 pytest 测试。
- 能规范使用分支、查看差异和编写信息明确的提交说明。

## 阶段 1.5 — LLM 应用桥接

**建议时间：** 2026 年 9 月下旬至 10 月

### 阶段目标

- 尽早完成真实模型调用，建立对请求、响应、非确定性输出、令牌、延迟和失败模式的直观认识。
- 把阶段 1 的类型、异常、重试、日志和异步能力用于真实 LLM 场景，而不是另写一次性演示脚本。
- 建立“模型行为也需要契约、测试和评测”的工程意识，为后端集成提供可复用的业务模块。

### 核心技能

- 使用原生模型 SDK/API 完成文本生成请求；理解指令、用户输入、模型配置、输出和 usage。
- 使用环境变量管理密钥，记录模型、令牌用量和延迟，不在代码、测试或日志中泄露密钥。
- 使用 JSON Schema 或 Pydantic 定义结构化输出，区分正常结果、拒绝、不完整响应、格式错误和 API 错误。
- 通过依赖注入或窄接口替换真实模型客户端，使默认单元测试不依赖网络、额度和模型随机性。
- 建立小型固定评测集，比较结构通过率、任务正确性、延迟、令牌和估算成本，避免只凭主观样例判断效果。

### 约束

- 优先学习原生 SDK/API；在理解请求生命周期、结构化输出和测试边界前，不引入 LangChain、LlamaIndex 或 Agent 框架。
- 默认 `pytest` 不访问真实模型；真实 API 调用必须显式开启、限制样例数量，并与单元测试分离。
- 第一轮只解决单次请求和结构化结果，不提前加入 RAG、工具调用、多智能体或复杂 Provider 抽象。
- 模型名称和超时等参数来自配置；需要可复现比较时固定模型版本，并保存评测条件。

### 阶段产出

- 一个具有清晰边界、类型标注、错误模型和测试替身的 LLM 对话分析模块。
- 一次受控的真实 API 冒烟验证，以及不依赖真实 API 的完整单元测试。
- 一组小型版本化评测样例和结果摘要，能够说明提示词修改是否真的改善了目标行为。

### 退出标准

- 能解释一次模型请求从输入构造到结果解析的完整生命周期，以及不同失败类型应由哪一层处理。
- 能在不访问真实模型的情况下测试业务逻辑，并能安全地执行少量真实端到端验证。
- 能使用固定样例和指标比较两个实现，而不是以“看起来不错”作为验收结论。

## 阶段 2 — 后端开发基础

**建议时间：** 2026 年 9 月下旬至 11 月

### 阶段目标

- 将阶段 1.5 的 LLM 对话分析模块放入真实 HTTP 服务中训练，避免另起与 AI 主线无关的纯 CRUD 示例。
- 独立开发结构合理、行为可测试的 Python 后端。
- 建立数据一致性、模型失败映射、流式响应和 API 接口契约意识。

### 核心技能

- HTTP、REST、状态码、FastAPI、Pydantic、依赖注入。
- 配置、日志、异常映射、认证基础、异步 API、流式响应与 SSE。
- SQL、PostgreSQL、事务、索引、JOIN、CTE、窗口函数。
- Redis 的基本缓存和限流场景，以及缓存失效风险。

### 阶段产出

- 一个逐步演进的 FastAPI LLM 服务：先封装阶段 1.5 的对话分析能力，再从内存记录演进到 PostgreSQL，并覆盖验证、冲突、模型失败和流式响应路径。
- 至少一次不依赖 ORM 的 SQL/事务训练，理解数据库实际行为。
- 单元测试和少量集成测试；默认测试使用模型替身，API 契约及关键设计取舍可解释。

### 退出标准

- 能设计常见 CRUD/API 行为，选择合理状态码并实现一致错误响应。
- 能解释模型超时或失败如何映射为 HTTP 行为，以及事务边界、索引取舍、连接管理和异步处理的实际价值。
- 能在不整体重写的情况下根据代码审查修复问题并补回归测试。

## 阶段 3 — LLM 应用可靠性与工具调用

**建议时间：** 2026 年 11 月至 12 月

### 阶段目标

- 在阶段 1.5 的单次请求和阶段 2 的服务化基础上，处理多轮上下文、流式响应、工具调用和生产可靠性。
- 从“可测试的模型功能”提升到“可验证、可观测、成本可控且能安全连接外部能力的 LLM 服务”。

### 核心技能

- 多轮消息、指令层级、令牌与上下文窗口、采样参数和上下文裁剪。
- 流式响应、工具与函数调用、参数验证和工具结果回传。
- 超时、重试、速率限制、幂等性、缓存、错误分类。
- 延迟、令牌用量与成本监控；提示词注入、工具权限和输出安全边界。

### 阶段产出

- 继续演进阶段 2 的后端，不另起一次性脚本。
- 至少一个有明确参数 Schema、权限边界、超时和错误模型的自定义工具。
- 一组覆盖多轮上下文、流式中断、工具成功与失败的回归评测，并持续比较质量、延迟和成本变化。

### 退出标准

- 能解释多轮上下文构造、裁剪、流式事件和工具调用的完整生命周期。
- 能区分可重试与不可重试错误，避免无限重试和不可控成本。
- 能在测试中不依赖真实 API 也验证工具编排与核心业务逻辑，并能进行少量受控端到端验证。

## 阶段 4 — RAG 工程

**建议时间：** 2026 年 12 月至 2027 年 1 月中旬

### 阶段目标

- 构建可评估、可引用、可迭代的 RAG，而不是只展示问答效果。
- 发挥应用统计背景，建立数据集、指标、实验设计和误差分析习惯。

### 核心技能

- 文档解析、分块、嵌入、余弦相似度、向量检索、pgvector。
- BM25、混合检索、元数据过滤、重排序、查询改写、引用。
- Recall@K、MRR、答案正确性、忠实度、引用正确性、延迟、成本。
- 离线评测集、基线、回归对比和失败案例分类。

### 阶段产出

- 一个基于真实小型文档集的 RAG 服务，支持来源引用和元数据过滤。
- 从简单检索基线逐步加入向量/混合检索；每次改动有对照结果。
- 小型版本化评测数据集、评测脚本和结果摘要。

### 退出标准

- 能解释分块、top-k、嵌入和重排序的取舍及常见失败模式。
- 能用指标和案例分析证明改动是否有效，而不是凭主观样例判断。
- 能追踪答案到来源，并区分检索失败与生成失败。

## 阶段 5 — 智能体基础

**建议时间：** 2027 年 1 月中旬至春节前后

### 阶段目标

- 在已有后端、LLM 和 RAG 基础上实现可控的单智能体工作流。
- 理解智能体是带状态、工具和停止条件的工程系统，而非无限自主循环。
- 整理可用于实习展示和面试讨论的端到端项目证据。

### 核心技能

- 工具调用、智能体循环、ReAct、路由、状态、记忆、上下文工程。
- 重试、降级、最大步数、人工介入、MCP 基础。
- 工具、轨迹和答案评估，安全边界与可观测性。
- 基础 Docker、环境配置、回归测试、链路追踪、CI 概念、延迟和成本优化。

### 阶段产出

- 一个围绕明确用户场景的单智能体，复用前期服务或 RAG 能力。
- 至少两个有清晰结构定义、错误模型和权限边界的工具。
- 覆盖成功、工具失败、无效参数、超步数和人工确认的测试/评估案例。
- 一份简洁项目说明：问题、架构、取舍、评测、限制和下一步。

### 退出标准

- 能画出并解释智能体循环、状态变化、工具选择、停止条件和失败恢复。
- 能使用固定任务集评估工具选择和最终答案，而不是只做成功演示。
- 能独立运行、调试和说明端到端系统，并回答常见后端、RAG、智能体、测试和 Git 面试问题。

## 路线调整规则

每周依据实际证据调整路线，而不是为了日期跳过基础：

- 连续出现同类错误：下周加入一个更小的针对性任务，并降低新概念数量。
- 某项达到稳定熟练：减少重复练习，把它作为后续任务的默认质量要求。
- 项目范围过大：拆成 30 分钟到 3 小时、可独立验收的任务。
- 时间落后：优先保留端到端主线、测试、评测和解释能力，削减框架数量与非核心进阶任务。
- 时间领先：优先加强失败路径、数据/评测质量、可观测性和工程取舍，不盲目增加新框架。
